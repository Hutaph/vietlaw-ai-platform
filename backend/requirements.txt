# === Core Web Framework ===
fastapi
uvicorn
pydantic
python-dotenv
python-multipart

# === LangChain + LLM ===
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-huggingface>=1.2.0
langchain-openai>=0.1.0
langchain-text-splitters>=0.2.0

# === Vector Store ===
# faiss-cpu is installed separately for local FAISS fallback paths.
qdrant-client>=0.11.0

# === Database ===
psycopg[binary]

# === HuggingFace Hub ===
huggingface_hub>=0.24.0

# === Local Model Runtime ===

# === Ingestion & Local Tools ===
# faiss-cpu is installed separately for local FAISS fallback paths.
tqdm>=4.66.0
